---
title: Multi-factor authentication
sidebar_position: 60
description: "Enhanced security setup with multi-factor authentication for Weaviate Cloud browser logins."
image: og/wcd/user_guides.jpg
---

Multi-factor authentication (MFA) increases the security of browser logins. MFA is not enabled by default.

## Enable multi-factor authentication

To enable MFA, follow these steps:

import EnableMFA from "/docs/cloud/img/weaviate-cloud-enable-mfa.png";
import MFA from "/docs/cloud/img/weaviate-cloud-mfa.png";

<div class="row">
  <div class="col col--4">
    <ol>
      <li>
        Open the{" "}
        <Link to="/go/console?utm_content=cloud">Weaviate Cloud console</Link>.
      </li>
      <li>Click on the user menu in the top-right corner of the console.</li>
      <li>
        Select <code>Account Settings</code>.
      </li>
    </ol>
  </div>
  <div class="col col--8">
    <div class="card">
      <div class="card__image">
        <img
          src={EnableMFA}
          alt="The user menu in the top-right corner of the Weaviate Cloud console, with Account Settings and Logout"
        />
      </div>
      <div class="card__body">
        The user menu in the top-right corner holds{" "}
        <code>Account Settings</code>.
      </div>
    </div>
  </div>
</div>
<br />
<div class="row">
  <div class="col col--4">
    <ol start="4">
      <li>
        In the <code>Multi-Factor Authentication</code> section, click{" "}
        <code>Enable MFA</code>.
      </li>
      <li>Open your authenticator application and scan the QR code.</li>
      <li>Enter the one-time code from your application.</li>
    </ol>
  </div>
  <div class="col col--8">
    <div class="card">
      <div class="card__image">
        <img
          src={MFA}
          alt="The Multi-Factor Authentication section of the Account page, with the Enable MFA button and the MFA status"
        />
      </div>
      <div class="card__body">
        The <code>Account</code> page shows the <code>Enable MFA</code> button
        and the current MFA status.
      </div>
    </div>
  </div>
</div>

<br />

After you configure MFA, Weaviate Cloud prompts you to supply the one-time authentication code each time you log in.

## Disable MFA

To disable MFA, [contact support](https://support.weaviate.io).

## MFA and applications

If you use a JavaScript/TypeScript client to connect a browser hosted application to Weaviate, do not enable MFA for that client's account.

There is no way to pass the one-time authentication code to the application, so the application cannot connect to Weaviate Cloud.

Use API keys to connect browser based client applications to Weaviate Cloud.

## Support

import SupportAndTrouble from "/_includes/wcs/support-and-troubleshoot.mdx";

<SupportAndTrouble />
