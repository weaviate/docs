import Link from "@docusaurus/Link";

To retrieve your API keys, follow these steps:

import Register from "/docs/cloud/img/weaviate-cloud-api-keys.png";

export const imageContent = (
  <ol>
    <li>
      Open the{" "}
      <Link to="https://console.weaviate.cloud">Weaviate Cloud console</Link>{" "}
      and{" "}
      <Link to="/docs/cloud/manage-clusters/status#select-your-cluster">
        select your cluster
      </Link>
      .
    </li>
    <li>
      On the <code>Cluster details</code> page, find the <code>API keys</code>{" "}
      section.
    </li>
    <li>Copy the needed key and store it in a safe location.</li>
  </ol>
);

<DocsImage
  image={Register}
  caption="Find your API key in Weaviate Cloud"
  layout="right"
>
  {imageContent}
</DocsImage>
