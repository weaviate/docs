import Link from "@docusaurus/Link";
import OrganizationSettings from "/docs/cloud/img/weaviate-cloud-organization-settings.png";

<div class="row">
  <div class="col col--4">
    <ol>
      <li>
        Open the{" "}
        <Link to="/go/console?utm_content=others">Weaviate Cloud console</Link>.
        You land on the <code>Overview</code> page.
      </li>
      <li>
        Use the <code>ORGANIZATION</code> selector in the top bar to pick the
        organization you want to edit.
      </li>
      <li>
        Expand the <code>Organization Settings</code> section on the{" "}
        <code>Overview</code> page.
      </li>
    </ol>
  </div>
  <div class="col col--8">
    <div class="card">
      <div class="card__image">
        <img
          src={OrganizationSettings}
          alt="The Organization Settings section on the Weaviate Cloud Overview page, showing users and roles and the organization actions"
        />
      </div>
      <div class="card__body">
        <code>Organization Settings</code> on the Weaviate Cloud{" "}
        <code>Overview</code> page.
      </div>
    </div>
  </div>
</div>
<br />
