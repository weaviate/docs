The Weaviate TypeScript client supports TypeScript and JavaScript.

The v3 client supports server side development (Node.js hosted). If your application is browser based, consider using the [TypeScript client v2](/docs/weaviate/client-libraries/typescript/typescript-v2).
