package main

// Placeholder for the quickstart's retrieval augmented generation (RAG) step.
//
// Generative/RAG queries are not yet available in the v6 Go client (there is no
// collection-level generative config and no generate query), so there is nothing
// runnable to show yet — the rendered quickstart tab shows the note below. This
// file stays a valid `package main` so the go6 build/vet lane keeps compiling
// this directory.
func main() {}

// START RAG
// Retrieval augmented generation (RAG) is coming soon to the v6 Go client.
// END RAG
