package main

import "testing"

// This file holds placeholders for the keyword (BM25) search how-to page. The v6
// Go client does not expose a standalone BM25 query yet — only the keyword
// component of a hybrid search (query.Hybrid) is available — so each marked
// region contains only a "Coming soon" line. The Go v6 doc tab renders that
// placeholder while the surrounding test compiles and skips.

// TestBM25Basic is a placeholder: a standalone BM25 keyword search is not yet
// available in the v6 Go client.
func TestBM25Basic(t *testing.T) {
	t.Skip("standalone BM25 keyword search is not yet available in the v6 Go client")

	// TODO[g-despot]: basic BM25 search snippet pending v6 client support
	// START BM25Basic
	// Coming soon
	// END BM25Basic
}

// TestBM25Score is a placeholder: retrieving BM25F scores requires the standalone
// BM25 query, which is not yet available in the v6 Go client.
func TestBM25Score(t *testing.T) {
	t.Skip("standalone BM25 keyword search is not yet available in the v6 Go client")

	// TODO[g-despot]: BM25 score-retrieval snippet pending v6 client support
	// START BM25Score
	// Coming soon
	// END BM25Score
}

// TestBM25Properties is a placeholder: restricting a BM25 search to selected
// properties requires the standalone BM25 query, not yet in the v6 Go client.
func TestBM25Properties(t *testing.T) {
	t.Skip("standalone BM25 keyword search is not yet available in the v6 Go client")

	// TODO[g-despot]: BM25 selected-properties snippet pending v6 client support
	// START BM25Properties
	// Coming soon
	// END BM25Properties
}

// TestBM25Boost is a placeholder: per-property BM25 weighting requires the
// standalone BM25 query, which is not yet available in the v6 Go client.
func TestBM25Boost(t *testing.T) {
	t.Skip("standalone BM25 keyword search is not yet available in the v6 Go client")

	// TODO[g-despot]: BM25 property-boosting snippet pending v6 client support
	// START BM25Boost
	// Coming soon
	// END BM25Boost
}

// TestBM25Limit is a placeholder: limit/offset on a standalone BM25 search is not
// yet available in the v6 Go client.
func TestBM25Limit(t *testing.T) {
	t.Skip("standalone BM25 keyword search is not yet available in the v6 Go client")

	// TODO[g-despot]: BM25 limit and offset snippet pending v6 client support
	// START BM25Limit
	// Coming soon
	// END BM25Limit
}

// TestBM25Autocut is a placeholder: autocut on a standalone BM25 search is not
// yet available in the v6 Go client.
func TestBM25Autocut(t *testing.T) {
	t.Skip("standalone BM25 keyword search is not yet available in the v6 Go client")

	// TODO[g-despot]: BM25 autocut snippet pending v6 client support
	// START BM25Autocut
	// Coming soon
	// END BM25Autocut
}

// TestBM25Filter is a placeholder: filtering a standalone BM25 search is not yet
// available in the v6 Go client.
func TestBM25Filter(t *testing.T) {
	t.Skip("standalone BM25 keyword search is not yet available in the v6 Go client")

	// TODO[g-despot]: BM25 filter snippet pending v6 client support
	// START BM25Filter
	// Coming soon
	// END BM25Filter
}
