:::warning Replication factor change

The replication factor of a class cannot be updated using `UpdateClass`. From `v1.32` by using [replica movement](/deploy/configuration/replica-movement), the replication factor and replicas location of a class can be changed.
:::

